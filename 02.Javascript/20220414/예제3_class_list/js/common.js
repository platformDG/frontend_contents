'use strict'
function textFunc(){
  // class 단일 추가
  // document.querySelector('#text').classList.add('change-bg');
  // class 다중 추가
  // document.querySelector('#text').classList.add('change-font-style','change-bg','change-width');
  // 삭제는 추가의 add대신에 remove사용한다.
  document.querySelector('#text').classList.remove('change-font-style','change-bg','change-width');
}
